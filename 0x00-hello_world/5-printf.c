#include <stdio.h>
/**
 *main - Entry point
 *Return: Always 0 (Success
**/
int main(void)
{
	char tmp[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", tmp);
	printf("\n");
	return (0);
}
